# 复制为 backend/.env 后填写（Docker Compose 会读取）
#
# —— 大脑（二选一或优先密钥）——
# 1) 云端 / 任意 OpenAI 兼容网关：填写密钥后启用（无需安装 Ollama）
# OPENAI_API_KEY=sk-你的密钥
# OPENAI_BASE_URL=https://api.openai.com/v1
# MODEL=gpt-4o-mini
#
# DeepSeek 官方 API（同样走 OpenAI 兼容接口）：到 https://platform.deepseek.com 申请 API Key
# OPENAI_API_KEY=sk-你的密钥
# OPENAI_BASE_URL=https://api.deepseek.com/v1
# MODEL=deepseek-chat
# 需要强推理时可试：MODEL=deepseek-reasoner（计费与限额以官网为准）
#
# 2) 本地 Ollama（不填 OPENAI_API_KEY 时生效）：先安装 Ollama，再 ollama pull deepseek-r1:7b
#    模型放 D 盘：设置用户环境变量 OLLAMA_MODELS=D:\Ollama\models 后重启 Ollama，再 pull（见 scripts/setup-ollama-d-drive.ps1）
# OLLAMA_BASE_URL=http://127.0.0.1:11434/v1
# OLLAMA_MODEL=deepseek-r1:7b
# OLLAMA_API_KEY=
# 公网部署时：CORS 允许的前端源，逗号分隔；默认 * 即可被任意 Vercel/Netlify 域名访问
# CORS_ORIGINS=*

# 学习材料（红帆「学习材料」页）：默认读取仓库根下「02教材5本」；服务器上可改为绝对路径
# LEARNING_MATERIALS_DIR=/var/data/02教材5本
