# 复制为 frontend/.env 供本地参考；勿把含真实密钥的文件提交 Git。
# Vercel / Netlify：在「项目 → Environment Variables」里配置同名变量后重新 Deploy（Vite 在构建时写入）。

# —— 评委使用公网 DeepSeek 的必配项 ——
# 1) 后端已部署 HTTPS，且在 Render/Railway 等平台配置了 OPENAI_API_KEY + DeepSeek 的 OPENAI_BASE_URL。
# 2) 下面填该后端的「根地址」，不要末尾斜杠。例：https://gdut-aid-api.onrender.com
VITE_API_BASE=

# 要调用真实 API 时，必须为 false 或不设置。设为 true 则整站静态演示、不调大模型。
VITE_DEMO_ONLY=false

# 仅当站点挂在子路径时设置（如 GitHub Pages /repo/）；根域名部署留空即可。
# VITE_BASE_PATH=/
